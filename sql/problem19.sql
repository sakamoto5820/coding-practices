-- 問19. location='Tokyo' の部署に所属し、1つ以上のプロジェクトにアサインされている
--       従業員の name, dept_name を name 昇順・重複なしで取得せよ。
-- テーブル: employees(id, name, department_id)
--           departments(id, dept_name, location)
--           assignments(employee_id, project_id)
-- TODO: ここにSQLを書く
SELECT NULL WHERE 0;
