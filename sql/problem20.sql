-- 問20. 2025年中に3回以上注文したユーザーの name, 注文回数, 合計金額 を
--       合計金額の降順・同額なら name の昇順で取得せよ。
-- テーブル: users(id, name, registered_at)
--           orders(id, user_id, amount, ordered_at)
-- TODO: ここにSQLを書く
SELECT NULL WHERE 0;
